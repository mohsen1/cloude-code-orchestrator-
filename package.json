{
  "name": "claude-code-orchestrator",
  "version": "1.0.0",
  "description": "A distributed system for orchestrating multiple Claude Code instances",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "build:all": "npm run build && npm run build:web",
    "prestart": "npm run build:all",
    "postinstall": "cd web && npm install && cd ..",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "dev:web": "npm --prefix web run dev",
    "build:web": "npm --prefix web run build",
    "lint:web": "npm --prefix web run lint",
    "test": "vitest",
    "test:run": "vitest run",
    "test:e2e": "tsx tests/e2e/run-e2e.ts",
    "lint": "eslint src/**/*.ts",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "claude",
    "orchestrator",
    "distributed",
    "ai"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "execa": "^9.6.1",
    "express": "^5.2.1",
    "glob": "^13.0.0",
    "simple-git": "^3.30.0",
    "winston": "^3.19.0",
    "yaml": "^2.8.2",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.17"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
